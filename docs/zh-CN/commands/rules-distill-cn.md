---
description: rules-distill 技能的遗留斜杠入口垫片。优先直接使用该技能。
---

# Rules Distill (遗留垫片)

仅当仍调用 `/rules-distill` 时使用此文件。维护的工作流位于 `skills/rules-distill/SKILL.md`。

## 规范入口

- 优先直接使用 `rules-distill` 技能。
- 仅保留此文件作为兼容入口点。

## 参数

`$ARGUMENTS`

## 委托

应用 `rules-distill` 技能并遵循其盘点、交叉阅读和判定工作流，而不是在此处重复该逻辑。
